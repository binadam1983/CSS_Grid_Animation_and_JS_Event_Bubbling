# CSS_Grid_Animation_and_JS_Event_Delegation


This repo uses the combination of following CSS and JS features to demonstrate an exciting page layout model/proto:

-- CSS Grid Animation using transition and some of SCSS features -- JS Event Delegation

Note: The app may not behave as intended on Chrome for not support CSS grid animations, but would work perfectly fine on Firefox.
